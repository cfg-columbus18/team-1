.navbar.navbar-light.fixed-top.navbar-expand-md
  .container
    a.navbar-brand.border(href='/' style='padding:1rem; margin:1rem;')
      | Refugee Road
    button.navbar-toggler(type='button', data-toggle='collapse', data-target='.navbar-collapse')
      span.sr-only Toggle navigation
      span.navbar-toggler-icon
    .collapse.navbar-collapse
      ul.nav.navbar-nav
        li.nav-item(class=(title === 'Home') ? 'active' : undefined)
          a.nav-link(href='/') Home
        li.nav-item(class=(title === 'Learn') ? 'active' : undefined)
          a.nav-link(href='/learn') Learn
        li.nav-item(class=(title === 'Find A Mentor') ? 'active' : undefined)
          a.nav-link(href='/find') Find A Mentor
        li.nav-item(class=(title === 'Forum') ? 'active' : undefined)
          a.nav-link(href='/connect') Forum
      ul.nav.navbar-nav.ml-auto
        if !user
          li.nav-item(class=(title === 'Log In') ? 'active' : undefined)
            a.nav-link(href='/login') Login
          li.nav-item(class=(title === 'Sign Up') ? 'active' : undefined)
            a.nav-link(href='/signup') Create Account
        else
          li.dropdown(class=(title === 'Account Management') ? 'active' : undefined).nav-item
            a.dropdown-toggle.nav-link(href='#', data-toggle='dropdown')
              if user.profile.picture
                img(src=user.profile.picture)
              else
                img(src=user.gravatar(60))
              span= user.profile.name || user.email || user.id
              i.caret
            ul.dropdown-menu
              li.dropdown-item
                a(href='/account') My Account
              li.divider.dropdown-item
              li.dropdown-item
                a(href='/logout') Logout
